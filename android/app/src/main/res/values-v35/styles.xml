<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Android 15+ (API 35): Edge-to-edge is enforced by default.
         Do NOT set statusBarColor or navigationBarColor here — these are deprecated on API 35.
         enableEdgeToEdge() in MainActivity.kt handles system bar appearance instead. -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:forceDarkAllowed">false</item>
        <item name="android:windowFullscreen">false</item>
        <!-- shortEdges/always deprecated on API 35; use default for proper edge-to-edge -->
        <item name="android:windowLayoutInDisplayCutoutMode">default</item>
    </style>

    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>
</resources>
